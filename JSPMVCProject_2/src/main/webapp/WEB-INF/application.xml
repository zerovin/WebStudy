<?xml version="1.0" encoding="UTF-8"?>
<!-- 
	클래스 등록 - 스프링
	SQL문장 등록 - MyBatis

	XML 단점 - 태그가 사용자 정의
	          문법이 엄격
	          반드시 루트(최대부모)는 한개만 사용 가능 => 트리헝태 구조 => 전체를 감싸는 하나의 태그가 있어야함
	          순서, 소속관계가 명확해야한다
	          반드시 열고 닫는 태그, 독립태그 < />
	          속성값 처리 "" 필수
	    장점 - 모든 언어에 호환
	          운영체제 호환
	          문서형 데이터베이스 => 시멘틱 태그
	            - 루트 => table명
	              ex) <sawon> => table
	                     <list> => row
	                        <name></name> => column
	                        <dept></dept>
	                        <job></job>
	                     <list>
	                     <list>
	                        <name></name>
	                        <dept></dept>
	                        <job></job>
	                     <list>
	                     <list>
	                        <name></name>
	                        <dept></dept>
	                        <job></job>
	                     <list>
	                 </sawon>
	      
	Vue, React => JSX (JavaScript + xml)
	          
-->
<beans> <!-- 클래스 여러개를 묶어서 저장 : 스프링 -->
	<bean id="list.do" class="com.sist.model.ListModel"/>
	<bean id="insert.do" class="com.sist.model.InsertModel"/>
	<bean id="update.do" class="com.sist.model.UpdateModel"/>
	<bean id="delete.do" class="com.sist.model.DeleteModel"/>
	<bean id="find.do" class="com.sist.model.FindModel"/>
</beans>